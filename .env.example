# WhatsApp Group Name (exact name as it appears in your WhatsApp)
GROUP_NAME=Your Group Name Here

# Output file path (optional, defaults to output/phone-numbers.json)
OUTPUT_FILE=output/phone-numbers.json

# Scraper approach: whatsapp-web, puppeteer, or playwright
SCRAPER_APPROACH=whatsapp-web
